spring:
  application:
    name: hello-spring-cloud-config
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/huanzhiyin/spring-cloud-config.git
          search-paths: respo
          username: huanzhiyin
          password: q789639456123

server:
  port: 8887

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/